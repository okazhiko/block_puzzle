#include <stdio.h>
#include <conio.h>

/*
int main() {
	int key;
	int flag = 1;

	while (flag) {
		if (_kbhit()) {
			key = _getch();
			if (key == 0 || key == 224) {
				key = _getch();
			}
			flag = 0;
		}
	}

	switch (key) {
	case 0x4b:
		printf("左が押されました。\n");
		break;
	case 0x4d:
		printf("右が押されました。\n");
		break;
	default:
		printf("左右以外が押されました。\n");
		break;
	}

	return 0;
}

*/